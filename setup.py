from setuptools import setup

setup(name="pycolor",
      version="1.2.1",
      description="A 16-bit coloring in terminal using Python",
      author="DuckTech",
      packages=["pycolor"],
      zip_safe=False)
